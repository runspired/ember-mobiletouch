import Ember from 'ember';

export default Ember.View.extend({
  /*
  tap: function() {
    console.log('view tap');
  },
  internalClick: function() {
    console.log('view internalClick');
  },
  checkChanged: function(e) {
    var time = new Date().getTime();
    console.log('check changed at '+time);
    if (e) {
      console.log('event '+e.type+' at '+e.timeStamp+' event-'+Ember.guidFor(e));
      if (e.originalEvent) {
        var oe = e.originalEvent;
        console.log('original event '+oe.type+' at '+oe.timeStamp+' event-'+Ember.guidFor(oe));
      }
    }
  },
  didInsertElement: function() {
    this.on('change', this, this.checkChanged);
    console.log('inserted checkboxes view');
  }
  */
});
