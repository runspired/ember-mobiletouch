/**!
 * This is actually just a mirror of draggable-item
 * done for expediency.
 */
import Component from "../draggable-item/component";

export default Component.extend({
  tagName: 'sortable-item'
});
